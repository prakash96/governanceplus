# Build output
target/
dependency-reduced-pom.xml

# Frontend build output copied into source by reviewer-web's Maven build
# (see reviewer-web/pom.xml copy-frontend-build execution) — generated, not authored.

# Local model files — commit only the split zips under models/chunks/
models/*.gguf
out/

# Personal Claude Code overrides — not shared with the team
.claude/settings.local.json